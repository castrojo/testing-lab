# CronWorkflow: polls ghcr.io/ublue-os/aurora:stable hourly.
# Triggers bluefin-qa-pipeline (system suite only — aurora uses KDE)
# only when OCI digest changes vs ConfigMap state.
apiVersion: argoproj.io/v1alpha1
kind: CronWorkflow
metadata:
  name: image-poll-aurora-stable
  namespace: argo
  labels:
    app.kubernetes.io/part-of: lab
    bluefin.io/trigger: digest-poll
spec:
  # Suspended 2026-08: aurora is a third-party lane not under review; QA fired
  # on every upstream digest change. Submit on demand via
  # `argo submit --from cronworkflow/image-poll-aurora-stable -n argo`.
  suspend: true
  schedules:
    - "27 */3 * * *"
  timezone: "UTC"
  concurrencyPolicy: Forbid
  startingDeadlineSeconds: 300
  workflowSpec:
    serviceAccountName: argo
    podGC:
      strategy: OnWorkflowCompletion
    ttlStrategy:
      secondsAfterSuccess: 3600
      secondsAfterFailure: 86400
    workflowTemplateRef:
      name: image-poller
    arguments:
      parameters:
        - name: image
          value: "ghcr.io/ublue-os/aurora"
        - name: image-tag
          value: "stable"
        - name: image-digest
          value: ""
        - name: state-key
          value: "digest-aurora-stable"
        - name: suites
          value: "system"
        - name: variant
          value: "aurora"
        - name: testsuite-repo
          value: "https://github.com/projectbluefin/testsuite"
